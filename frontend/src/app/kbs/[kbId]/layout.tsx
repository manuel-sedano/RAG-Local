import { KbWorkspaceLayout } from "@/components/kb-workspace-layout";

export default function KbIdLayout({ children }: { children: React.ReactNode }) {
  return <KbWorkspaceLayout>{children}</KbWorkspaceLayout>;
}
