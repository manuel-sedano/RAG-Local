# Plantilla para tests / CI (ejemplo). Copia a `.env.test` si tu pipeline lo requiere.
# Valores pensados para Docker Compose (servicios en la misma red) o localhost.

ENVIRONMENT=test
LOG_LEVEL=WARNING

# En `test`, el backend puede autocompletar JWT vacío; en CI fija uno largo.
JWT_SECRET=

DATABASE_URL=postgresql+psycopg://rag:rag_password_local@postgres:5432/rag_test
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=rag_test
POSTGRES_USER=rag
POSTGRES_PASSWORD=rag_password_local

REDIS_HOST=redis
REDIS_PORT=6379
QDRANT_HOST=qdrant
QDRANT_PORT=6333
OLLAMA_HOST=ollama
OLLAMA_PORT=11434

CORS_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
HEALTH_HTTP_TIMEOUT_SECONDS=5
